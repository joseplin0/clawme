interface:
  display_name: "Task"
  short_description: "Run the ClawMe /task issue-to-code workflow"
  default_prompt: "Use $task to execute the ClawMe /task workflow for this requirement. Follow SKILL.md strictly, continue an existing issue instead of creating a duplicate when one is provided, and use an issue-linked branch/worktree plus a draft PR for new work."
