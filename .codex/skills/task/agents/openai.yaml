interface:
  display_name: "Task"
  short_description: "Run the ClawMe /task issue-to-code workflow"
  default_prompt: "Use $task to execute the ClawMe /task workflow for this requirement. Follow SKILL.md strictly, and continue an existing issue instead of creating a duplicate when the user already provides one."
